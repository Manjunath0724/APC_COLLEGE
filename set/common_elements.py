# Create two sets
set_a = {10, 20, 30, 40}
set_b = {30, 40, 50, 60}

# Find the elements common to both sets (intersection)
common_elements = set_a.intersection(set_b)

# Display the sets and common elements
print("Set A:", set_a)
print("Set B:", set_b)
print("Common elements:", common_elements)
